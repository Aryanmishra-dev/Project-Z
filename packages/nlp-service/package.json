{
  "name": "@project-z/nlp-service",
  "version": "1.0.0",
  "scripts": {
    "dev": "./venv/bin/uvicorn app.main:app --reload --port 8000",
    "start": "./venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "pytest"
  }
}
